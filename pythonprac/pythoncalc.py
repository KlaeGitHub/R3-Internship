first = input("first: ")
second = input("second: ")
' allows user to input a number'
sum = float(first) + float(second)
'adds first input to second, we need to put <float> so it turns it into a integer which can be decimals aswell. '
print(sum)
' prints the answer'